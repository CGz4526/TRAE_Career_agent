# ============================================================
# 生产环境前端环境变量示例（Vercel 部署用）
# ------------------------------------------------------------
# 注意：此文件仅作说明，不要提交真实域名。
# 请在 Vercel 控制台 → 项目 → Settings → Environment Variables
# 中设置以下两项（Production / Preview 都勾选）：
#
#   VITE_API_BASE   = https://你的Java服务域名.railway.app
#   VITE_AGENT_BASE = https://你的Python服务域名.railway.app
#
# Vite 在构建时会将这些变量编译进前端包（build-time 注入），
# 改域名需重新部署。本地开发不设置这两项，前端自动走 vite proxy。
# ============================================================

# Java 后端（/api 业务接口）部署后的真实域名
VITE_API_BASE=https://your-java-service.railway.app

# Python AI 服务（/agent 智能接口）部署后的真实域名
VITE_AGENT_BASE=https://your-python-service.railway.app
